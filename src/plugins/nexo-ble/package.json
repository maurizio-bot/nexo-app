{
  "name": "capacitor-nexo-ble",
  "version": "1.0.0",
  "description": "NEXO BLE Plugin - Nordic based",
  "main": "dist/plugin.js",
  "module": "dist/plugin.mjs",
  "types": "dist/plugin.d.ts",
  "scripts": {
    "build": "npm run clean && tsc && rollup -c rollup.config.js",
    "clean": "rimraf ./dist",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/android": "^5.7.0",
    "rimraf": "^5.0.0",
    "rollup": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "capacitor": {
    "android": {
      "src": "android"
    }
  }
}
