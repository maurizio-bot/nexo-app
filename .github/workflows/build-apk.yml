name: NEXO APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Install Dependencies
        run: npm ci
      
      - name: Build Web Only
        run: npm run build:web
      
      - name: Pre-configure AndroidX (Pre-Sync)
        run: |
          mkdir -p android
          rm -f android/gradle.properties
          echo "org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8" > android/gradle.properties
          echo "org.gradle.daemon=false" >> android/gradle.properties
          echo "android.useAndroidX=true" >> android/gradle.properties
          echo "android.enableJetifier=true" >> android/gradle.properties
          echo "=== Pre-sync AndroidX OK ==="
          cat android/gradle.properties
      
      - name: Sync Capacitor
        run: npx cap sync android
      
      - name: Emergency AndroidX Override (Post-Sync)
        working-directory: ./android
        env:
          JAVA_HOME: ${{ env.JAVA_HOME }}
        run: |
          echo "=== Verificando post-sync ==="
          cat gradle.properties || echo "ARCHIVO NO EXISTE"
          
          rm -f gradle.properties
          echo "org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8" > gradle.properties
          echo "org.gradle.daemon=false" >> gradle.properties
          echo "android.useAndroidX=true" >> gradle.properties
          echo "android.enableJetifier=true" >> gradle.properties
          echo "org.gradle.java.home=$JAVA_HOME" >> gradle.properties
          chmod +x gradlew
          echo "=== AndroidX Forzado OK ==="
          cat gradle.properties
      
      - name: Build Android Debug APK
        working-directory: ./android
        run: |
          if ! grep -q "android.useAndroidX=true" gradle.properties; then
            echo "::error:: AndroidX no está habilitado!"
            exit 1
          fi
          ./gradlew clean assembleDebug --stacktrace
      
      # REEMPLAZAR ESTE STEP - ELIMINA upload-artifact Y USA RELEASES:
      - name: Create Release with APK
        if: success()
        uses: softprops/action-gh-release@v1
        with:
          tag_name: nightly-${{ github.run_number }}-${{ github.sha }}
          name: "NEXO Nightly Build #${{ github.run_number }}"
          prerelease: true
          files: android/app/build/outputs/apk/debug/app-debug.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # OPCIONAL: Backup temporal con retención mínima (borrar en 1 día si quieres conservar el híbrido)
      # - name: Upload Artifact (Backup 1 día)
      #   if: failure()  # Solo si release falla
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: nexo-debug-apk-fallback
      #     path: android/app/build/outputs/apk/debug/app-debug.apk
      #     retention-days: 1
